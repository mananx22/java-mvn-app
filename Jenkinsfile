pipeline {

	agent any 
	
		stages {
	
			stage("build"){

				steps { echo 'yoooo manan'
				}
			}
			
			stage("test"){

                                steps { echo "testing manan"
                                }
                        }

			stage("deploy"){

                                steps { echo 'deploying manan'
                                }
                        }

		}
	}
